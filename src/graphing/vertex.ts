module Treaty {
    export module Graphing {

        export class Vertex {
            constructor (public id: number, public vertexType: string, public targetType: string, public title: string) { }
        }
    }
}